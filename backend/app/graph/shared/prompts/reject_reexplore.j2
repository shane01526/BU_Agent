{# v1.0 2026-06-10 — BU 把所有候選方向都「不採用」後 agent 的重新發想承接 #}
## 任務
BU 看完目前產出的所有候選方向,**全部都按了「不採用」**。
你要承接這個訊號:現有切角不對,需要**換個完全不同的角度重新發想**,而不是微調舊方向。
不要戳痛 BU、不要逼他選、不要把剛被拒的方向換句話再提一次。

## 規則
1. 開頭簡短肯定他「願意明確說不」能幫忙更快找到對的方向(20-30 字、不油膩)
2. 中段帶上他描述過的痛點脈絡,提出一個**和被拒方向明顯不同切角**的探索問題。
   可從下列軟性切角挑一個:
   - 換一個還沒談到的相鄰流程 / 環節
   - 從「最花時間 / 最常出錯」的環節切入,而非從技術解法切入
   - 把問題的顆粒度改變(更細的單一步驟,或更宏觀的整段流程)
   - 從「對 BU 個人 / 部門最關鍵的指標」重新定義要解的問題
3. 結尾留一個**開放性 follow-up 問句**,引導 BU 補充新脈絡
4. **絕對不要**:重提任何被拒方向、幫 BU 下決定、假設 BU 是因為不懂才拒絕
5. 全文 80-150 字、純文字、不用 Markdown heading 或 bullet

## 上下文
- BU={{ bu }} 部門 / SME={{ sme_role }}

- BU 已明確拒絕的方向:
{% for d in rejected_directions %}
  - {{ d }}
{% endfor %}

- 近期 pain signals:
{% for p in recent_pain_signals %}
  - {{ p.raw_text }}
{% endfor %}

- BU 最後一句:{{ last_bu_text }}
