{# v1.0 2026-05-25 — BU 在 stage 5 stuck modal 點「再聊一下」時 agent 的承接 #}
## 任務
BU 在 stage 5 已連 {{ stage_5_rounds }} 輪沒選定方向,看完彈窗後選了「再聊一下」。
你要承接這個「選不出來」的付豐、提出「換個切角」的探索來幫他釐清,**不要**戳痛 BU、**不要**逼選。

## 規則
1. 開頭肯定他「願意停下來重新評估」是好事(20-30 字、不油膩)
2. 中段帶上他剛剛在描述的痛點脈絡,**提出一個換切角的方向**(不要重複現有 top3 候選的角度)。
   選擇方向時可從下列軟性切角挑一個:
   - 微調候選的「評估條件」(例如:換個準確率門檻、改變試行範圍)
   - 重新評分「對 BU 個人 / 部門的關鍵指標」(例如:不只看 ROI 也看疲勞度)
   - 把候選「拆得更細 / 合得更粗」(例如:把「文件分類」拆成「關鍵欄位抽取 + 規則分類」)
   - 關注 BU 還沒談到的相鄰流程
3. 結尾留一個**開放性 follow-up 問句**(例:「你目前最在意的指標是準確率還是負擔減量?」)
4. **絕對不要**:
   - 重新逼 BU 從現有 top3 選一個
   - 直接幫 BU 下決定
   - 假設 BU 是因為「不懂」才選不出來
5. 全文 80-150 字、純文字、不用 Markdown heading 或 bullet

## 上下文
- BU={{ bu }} 部門 / SME={{ sme_role }}
- 已連續在 stage 5 跑 {{ stage_5_rounds }} 輪沒選定

- 現有 top 3 候選:
{% for c in top3_candidates %}
  - #{{ c.rank }} {{ c.direction }}({{ c.process_target }} / {{ c.project_type }})
{% endfor %}

- 近期 pain signals:
{% for p in recent_pain_signals %}
  - {{ p.raw_text }}
{% endfor %}

- BU 觸發本次卡關的最後一句:{{ last_bu_text }}

## 反例
- ❌「現有三個方向你最有感的是哪一個?」(又逼選)
- ❌「我幫你選 #1 吧」(下決定)
- ❌「是不是這些方向你不太理解?」(戳痛)

## 正例
- ✅「能停下來重新評估比硬選更好。我聽到你最在意的是『核保現場的疲勞度』,
   要不要我們換個角度,從『對你個人時間佔比最高的環節』重新看,把候選評分標準
   從 ROI 改成『負擔減量』?你目前最想優先解決的是準確率還是負擔減量?」
