# 軟體專案 Code Review 標準作業流程 (SOP) 指南

這份文件旨在提供一份完整且具專業水準的 Code Review (程式碼審查) 執行流程。遵循這套流程能有效提升團隊協作效率與系統穩定性。

在實務執行上，**我們絕對不會全靠人工一行一行看**。最佳實踐是：「**讓機器處理規範與語法，讓人類專注於邏輯與架構**」。

---

## 壹、 人工審查 vs. 工具自動化 (協作模式)

為了極大化效率，整個流程會明確劃分人工與工具的職責：

*   **機器自動化 (CI/CD Pipeline)：** 負責抓出語法錯誤、排版不一致、型別錯誤、已知資安漏洞、以及執行自動化測試。這應該在人工審查前就完成。
*   **人類審查 (Manual Review)：** 負責檢視商業邏輯是否正確、架構設計是否具備擴充性（例如：AI 代理之間的狀態傳遞流程是否合理、雲端基礎設施如 AWS 資源的配置是否最佳化）、以及程式碼的易讀性。

---

## 貳、 審查前的準備 (Pre-review Phase) - 【全自動化為主】

在進入人工審查程式碼之前，應確保基礎設施已過濾掉低級錯誤。

1.  **確認 PR/MR 描述完整性** 【人工撰寫/工具輔助】
    * 說明解決什麼問題、變更範圍。實務上也可導入 AI 工具自動摘要 PR 內容。
2.  **通過自動化流水線 (CI Check)** 【完全依賴工具】
    * **自動排版與風格檢查 (Formatter & Linter)：**
        * *常見工具：* `Black` (針對 Python)、`Ruff`、`Flake8`、`ESLint`。
        * 只要排版不符，CI 直接報錯，**人類不需要在 Code Review 時爭論縮排或空行**。
    * **靜態型別檢查 (Type Checking)：**
        * *常見工具：* `mypy` (Python)、`TypeScript` 編譯器。
    * **單元測試 (Unit Testing)：**
        * *常見工具：* `pytest`、`Jest`。搭配 Coverage 工具確保測試覆蓋率不下降。

---

## 參、 系統化審查流程 (Review Execution Phase) - 【人工審查為主】

當自動化檢查全數通過後，Reviewer 才會打開託管平台 (如 GitHub/GitLab) 的介面，查看高亮顯示的程式碼差異 (Diff)。此階段依序關注以下五個層面：

### 1. 商業邏輯與功能完整性 (Functionality) 【人工審查】
* **重點：** 程式碼是否真的實作了需求規格？
* **細節：** 考慮邊界條件 (Edge Cases)、異常處理。複雜的流程（如跨模組資料互動、多 Agent 的工作流設計），必須靠人工判斷邏輯是否合理。

### 2. 架構設計與設計模式 (Architecture & Design) 【人工審查】
* **重點：** 系統是否具備擴充性與可維護性？
* **細節：** 檢查單一職責 (SOLID)、是否抽出共用元件 (DRY)。評估目前的寫法是否會造成未來的技術債。

### 3. 安全性與效能 (Security & Performance) 【工具掃描 + 人工覆核】
* **工具掃描：** 
    * *常見工具：* `SonarQube` (程式碼品質與漏洞)、`Bandit` (Python 資安掃描)、`AWS CodeGuru` (針對效能與資安的自動建議)。
    * 工具會先過濾硬編碼 (Hardcode) 密碼、基礎注入風險等。
* **人工覆核：** 檢查工具掃不到的深層架構問題，如資料庫連線是否確實釋放、是否有記憶體洩漏風險、或大數據量下的演算法效能瓶頸。

### 4. 程式碼可讀性與慣例 (Readability & Style) 【人工審查】
* **重點：** 寫給「人」看的邏輯是否清晰？
* **細節：** 排版已由工具處理，此處人工只看「命名規範」是否具備描述性，以及複雜邏輯是否補充了說明「為什麼 (Why)」這樣寫的註解。

### 5. 測試的完整性 (Testing) 【人工審查】
* **重點：** 開發者寫的測試案例是否有意義？
* **細節：** 檢查是否涵蓋了「失敗路徑 (Unhappy Path)」，而不僅僅是驗證一定會成功的快樂路徑 (Happy Path)。

---

## 肆、 溝通與回饋原則 (Feedback Protocol)

Code Review 是技術交流而非針對個人。

1.  **區分嚴重程度**
    * **[Must] (必須修改)：** 涉及 Bug、資安、效能或嚴重邏輯錯誤。
    * **[Suggestion] (建議)：** 有更好的寫法，尊重作者最終決定。
    * **[Nitpick] (瑣碎點)：** 不影響功能的細節優化。
2.  **建設性提問**
    * 嘗試說：「我擔心這裡如果遇到空值會導致 Crash，有考慮加個 Null Check 嗎？」
3.  **讚美優秀程式碼**
    * 不吝給予 Positive Feedback。

---

## 伍、 結案與合併 (Finalization Phase)

1.  **修復與追蹤：** 開發者針對 Reviewer 的意見修改。大改動應重新觸發 CI。
2.  **確認解決 (Resolved)：** 所有「Must」留言必須達成共識。
3.  **核准與合併 (Approve & Merge)：** Reviewer 點擊 Approve，推薦使用 Squash and Merge 保持 Commit 歷史整潔。
